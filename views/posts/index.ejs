<%- include('../partials/header'); -%>

<div class="container-fluid">
	<% if(currentUser){ %>
		<div class="jumbotron text-center">
			<div class="container">
				<div class="row justify-content-center">
					<h1>Welcome,
						<%= currentUser.username %>!</h1>
				</div>
				<div class="row justify-content-center">
					<p>You've reached the admin panel. From here, you can add a post.</p>
				</div>
				<div class="row justify-content-center">
					<p>
						<a class="btn shampoo-gradient" href="/posts/new" role="button">
							<i class="fas fa-pencil-alt"></i> Add New Post</a>
					</p>
				</div>
			</div>
		</div>
		<% } %>

			<div class="row">
				<% posts.reverse().forEach(function(post){ %>
					<div class="col-md-2 col-sm-6">
						<div class="card card-overlay slender" style="max-width: 25rem;">
							<div class="view view-overlay">
							<img class="card-img-top" src="<%= post.image %>">
								</div>
							<div class="card-body text-center">
								<h4 class="card-title">
									<%= post.name %>
								</h4>
							
							<p>
								<a href="/posts/<%= post._id %>" class="btn shampoo-gradient">More Info</a>
							</p>
								</div>
						</div>
					</div>
					<% }); %>
			</div>
</div>
    
    
<%- include('../partials/footer'); -%>